<p id="notice"><%= notice %></p>

<h1>Holidays</h1>

<table>
  <thead>
    <tr>
      <th class="holiday_date">Holiday date</th>
      <th>Name</th>
      <th class="description">Description</th>
      <th class="centered">Country</th>
      <th class="centered">States</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @holidays.each do |holiday| %>
      <tr>
        <td><%= holiday.holiday_date %></td>
        <td><%= holiday.name %></td>
        <td><%= holiday.description %></td>
        <td class="centered"><%= holiday.country %></td>
        <td class="centered"><%= holiday.states.gsub(',',', ') %></td>
        <td><%= link_to 'Show', holiday %></td>
        <td><%= link_to 'Edit', edit_holiday_path(holiday) %></td>
        <td><%= link_to 'Destroy', holiday, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Holiday', new_holiday_path %>
